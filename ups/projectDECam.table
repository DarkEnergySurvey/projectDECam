
# Pyfits
setupRequired(pyfits 3.4+0)

# despyastro loads: python, numpy, matplotlib and pyfits
setupRequired(despyastro 3.0.1+1)

# DrawDEcam to draw ellipses
setupRequired(drawDECam 3.0.0+0)

# pil > 1.1.7+9 are compatible with libjpeg 8d+0 for tiff and stiff
setupRequired(pillow 8.0.1+2)

# Bertin's software
setupRequired(stiff 2.7.0+2)
setupRequired(swarp 2.40.1+0)

envAppend(PATH, ${PRODUCT_DIR}/bin)
envAppend(PYTHONPATH, ${PRODUCT_DIR}/python/)
